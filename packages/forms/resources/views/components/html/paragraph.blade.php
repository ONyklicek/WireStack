{{-- Html::paragraph(). Plain prose between fields — the text is escaped, which
     is what separates this factory from content(), where the caller supplies
     trusted markup of their own. --}}
<p class="text-sm text-gray-600 dark:text-gray-400">{{ $text }}</p>
