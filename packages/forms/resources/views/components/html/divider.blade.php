{{-- Html::divider(). The factory decides that a divider is wanted; the rule
     itself is markup and lives here, where it can be published and overridden
     like every other view this package ships. --}}
<hr class="my-4 border-gray-200 dark:border-gray-700">
