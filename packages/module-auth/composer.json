{
    "name": "nyoncode/wire-module-auth",
    "description": "The signed-out screens for wire: login, password reset, email verification and the two-factor challenge, over Laravel Fortify.",
    "keywords": [
        "laravel",
        "livewire",
        "wire",
        "wirestack",
        "nyoncode",
        "auth",
        "authentication",
        "fortify",
        "login",
        "two-factor"
    ],
    "homepage": "https://wirestack.nyoncode.cz",
    "type": "library",
    "license": "MIT",
    "authors": [
        {
            "name": "Ondrej Nyklicek",
            "role": "Developer"
        }
    ],
    "support": {
        "issues": "https://github.com/ONyklicek/WireStack/issues",
        "source": "https://github.com/NyonCode/wire-module-auth",
        "docs": "https://wirestack.nyoncode.cz"
    },
    "require": {
        "php": "^8.2",
        "nyoncode/wire-core": "self.version",
        "nyoncode/wire-forms": "self.version",
        "illuminate/support": "^12.0|^13.0",
        "laravel/fortify": "^1.30|^2.0",
        "nyoncode/laravel-package-toolkit": "^2.5.0"
    },
    "require-dev": {
        "orchestra/testbench": "^10.0|^11.0",
        "laravel/pint": "^1.29",
        "pestphp/pest": "^4.0|^5.0"
    },
    "suggest": {
        "nyoncode/wire-admin": "Supplies the frame these screens render in, so the login page matches the panel."
    },
    "autoload": {
        "psr-4": {
            "NyonCode\\WireModuleAuth\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "NyonCode\\WireModuleAuth\\Tests\\": "tests/"
        }
    },
    "extra": {
        "laravel": {
            "providers": [
                "NyonCode\\WireModuleAuth\\WireModuleAuthServiceProvider"
            ]
        }
    },
    "minimum-stability": "dev",
    "prefer-stable": true,
    "config": {
        "allow-plugins": {
            "pestphp/pest-plugin": true
        }
    }
}
