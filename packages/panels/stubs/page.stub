<?php

declare(strict_types=1);

namespace {{ namespace }};

{{ imports }}

/**
 * {{ title }} — a page of the application's own, drawn with the panel's heading,
 * trail and header actions around `{{ view }}`.
 *
 * {{ registration }}
 */
class {{ class }} extends Page{{ implements }}
{
{{ traits }}    protected static string $view = '{{ view }}';
{{ titleDeclaration }}{{ body }}}
