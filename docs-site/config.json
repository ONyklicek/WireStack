{
    "_comment": "Canonical docs-site matrix of locales x versions. The deploy job owns ONE copy of this file and feeds it to every branch build (DOCS_VERSIONS_FILE), so the switchers never drift between the 1.x and 2.x branches. build.php falls back to the values baked here when no env override is set.",
    "baseUrl": "https://wirestack.nyoncode.cz",
    "locales": [
        { "code": "en", "label": "English", "path": "", "default": true },
        { "code": "cs", "label": "Čeština", "path": "cs" }
    ],
    "versions": [
        { "label": "v1.x", "badge": "Latest", "path": "", "branch": "1.x", "available": true },
        { "label": "v2.x", "badge": "Soon", "path": "v2", "branch": "2.x", "available": false }
    ]
}
