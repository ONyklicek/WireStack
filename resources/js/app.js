import 'vite/modulepreload-polyfill';

// TiptapEditor's JS now ships pre-bundled inside the wire-forms package and is
// injected automatically by its Blade view — no import needed here. See
// packages/forms/resources/js/tiptap-editor.js (source) and `npm run build:forms-assets`.
